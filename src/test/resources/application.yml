spring:
  application:
    name: stock_auth_svc
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  datasource:
    auth:
      url: jdbc:h2:mem:stock_auth_test_db;DB_CLOSE_DELAY=-1
      username: sa
      password: sa
      driver-class-name: org.h2.Driver
    portfolio:
      url: jdbc:h2:mem:stock_portfolio_test_db;DB_CLOSE_DELAY=-1
      username: sa
      password: sa
      driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 60000 # 1 minute in milliseconds
  refresh-token:
    expiration: 120000 # 2 minutes in milliseconds

logging:
  level:
    com.sahil.stock.auth: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
